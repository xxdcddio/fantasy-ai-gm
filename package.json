{
  "name": "fantasy-ai-gm",
  "version": "0.1.0",
  "description": "Deterministic Yahoo Fantasy MLB general-manager tool.",
  "private": true,
  "scripts": {
    "analyze": "node scripts/analyze.js",
    "import": "node scripts/import.js",
    "player": "node scripts/player.js",
    "compare": "node scripts/compare.js",
    "coach": "node scripts/coach.js",
    "test": "for f in analyzer/*.test.js analyzer/models/*.test.js analyzer/providers/*.test.js scripts/*.test.js; do node \"$f\" || exit 1; done"
  }
}
